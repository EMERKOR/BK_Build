# Ball Knower Development Dependencies
#
# Install with: pip install -r requirements-dev.txt

# Testing framework
pytest>=7.0.0
pytest-timeout>=2.1.0

# Core dependencies (if not already installed)
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.0.0
