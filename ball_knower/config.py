"""
Shim config module for Ball Knower.

On this branch, this simply re-exports everything from src.config
so that `from ball_knower import config` works.
"""

from src.config import *  # type: ignore
